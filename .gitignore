node_modules/
dist/

# Keep dependency resolution reproducible for this Node project.
!package-lock.json

# Local secrets
.env
.env.*
!.env.example

# Local SQLite runtime data
prisma/*.db
prisma/*.db-journal
prisma/*.db-shm
prisma/*.db-wal

# Local editor/app state (Claude Terminal dashboard cache)
.claude-terminal

# Logs and temporary files
*.log
tmp_*.ts
tmp_*.png
tmp_*.json
*.zip
host-package/

# Local database backups
backups/*.json

# Lavalink runtime artifacts downloaded on first start
Lavalink.jar
lavalink/plugins/
lavalink/logs/
lavalink-host/plugins/
lavalink-host/logs/

# Ke hoach va tai lieu noi bo: giu tren may, khong day len repo public.
# Repo nay la public — plans/ va reports/ chua so do he thong, ID kenh/role va
# cac cho yeu ve bao mat. Saly da xoa chung khoi git ngay 2026-08-26.
plans/
docs/
